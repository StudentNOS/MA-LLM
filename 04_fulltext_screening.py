# Input: List of papers
# Output: Final selection of papers based on full-text screening


def get_fulltext(list_abstracts):
    """Get full-texts from papers based on abstracts
    Returns:
        list: List of full-texts
    """

def exclude_fulltext(fulltexts):
    """Exclude full-texts based on exclusion criteria
    Returns:
        list: List of full-texts
    """
